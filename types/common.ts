// types/common.ts
// Poderia conter tipos compartilhados, como:
// export type UserRole = 'student' | 'mentor' | 'admin';

// Por enquanto, pode ficar vazio ou podemos decidir mover AuthUser para cá depois.
export { }; // Para evitar erro de arquivo vazio em TypeScript